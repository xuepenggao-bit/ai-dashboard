name = "ai-dashboard-sync"
main = "portfolio-sync-worker.js"
compatibility_date = "2026-07-11"

# 在 Cloudflare 控制台的 Worker 设置中添加：
# - Secret: GITHUB_TOKEN
# - Variable: ALLOWED_ORIGIN = https://financial.qiwu.fun
